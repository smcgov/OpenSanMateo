<?php
/**
 * Implementation of hook_ctools_plugin_directory().
 */
function twitter_pull_pane_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools' && !empty($plugin)) {
    return "plugins/$plugin";
  }
}
