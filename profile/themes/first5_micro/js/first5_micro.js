Drupal.smc = Drupal.smc || {};

(function($) {
  
  Drupal.smc.customFunction = function() {
    // custom function here
  }

  Drupal.behaviors.customDrupalBehavior = {
    attach: function (context) {
      
     // jQuery here
     
    }
  };

})(jQuery);